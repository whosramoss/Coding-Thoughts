<?php
    $name = "   Jhon Doe   ";
    echo(strlen($name));
    $new = trim($name) 
    # ltrim = delete the spaces at the beginning
    # rtrim = delete trailing spaces
    echo ($new); 	
    echo(strlen($new));
?>